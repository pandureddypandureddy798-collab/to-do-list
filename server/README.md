# Todo Server

This folder contains the Express backend for the MERN To-Do application.

Quick start:

1. Copy `.env.example` to `.env` and fill values.
2. Install dependencies: `npm install`
3. Start dev server: `npm run dev`
